name: EPG Multi-File Sync to GitHub & Gitee

on:
  schedule:
    - cron: '0 */6 * * *'  # 每 6 小时运行
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: pip install requests

    - name: Run EPG Sync Script
      id: sync
      run: |
        # 运行脚本，并将输出重定向到日志文件
        python sync.py > sync.log 2>&1
        cat sync.log
        # 从输出中判断是否有变化，并设置环境变量
        if grep -q "🎉 有文件更新" sync.log; then
          echo "changed=true" >> $GITHUB_ENV
        else
          echo "changed=false" >> $GITHUB_ENV
        fi

    - name: Configure Git
      run: |
        git config --global user.name "GitHub Actions Bot"
        git config --global user.email "actions@github.com"

    - name: Commit Changed Files
      if: env.changed == 'true'
      run: |
        # 💡 新增：清除所有未跟踪的文件和目录，确保仓库干净
        git clean -fd      
        echo "🔍 检查哪些文件变化..."
        FILES_TO_COMMIT=$(cat files_to_commit.txt)
        echo "待提交文件列表: $FILES_TO_COMMIT"
        
        # --- 确保只添加所需文件 ---
        git add $FILES_TO_COMMIT
        
        if git diff --cached --quiet; then
          echo "No changes to commit."
          echo "PUSH_REQUIRED=false" >> $GITHUB_ENV
        else
          TIMESTAMP=$(date "+%Y-%m-%d %H:%M:%S")
          git commit -m "Auto sync EPG files: $TIMESTAMP"
          echo "PUSH_REQUIRED=true" >> $GITHUB_ENV
          
          # 💡 新增：强制 Git 重新打包对象，这有助于解决数据流卡住的问题
          echo "📦 强制重新打包 Git 对象..."
          git repack -a -d
        fi

    - name: Push to Gitee
      if: env.PUSH_REQUIRED == 'true'
      timeout-minutes: 5
      env:
        GITEE_USERNAME: shiwenhua
        GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        GITEE_REPO: shiwenhua/epg-sync
      run: |
        echo "🚀 推送至 Gitee..."
        # 使用更简洁的命令，明确指定 HEAD:master
        git push -f https://${GITEE_USERNAME}:${GITEE_TOKEN}@gitee.com/${GITEE_REPO}.git HEAD:master
        echo "PUSH_STATUS=success" >> $GITHUB_ENV

    - name: Push to GitHub
      if: success() && env.PUSH_REQUIRED == 'true'
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.ref }}

    - name: Final Status
      run: |
        if [ "${{ env.PUSH_REQUIRED }}" == "true" ]; then
          echo "🎉 文件同步成功并已推送到 Gitee 和 GitHub!"
        else
          echo "ℹ️ 文件内容未变化，同步已跳过。"
        fi
